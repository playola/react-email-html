import React from 'react';
import Header from './Header';

export default {
  title: 'Core/Header',
  component: Header,
};

export const Default = () => (
  <Header>
    This is the default email Header
  </Header>
);
